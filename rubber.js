class rubber{
    constructor(x,y){
    var option={
    'density':0.3,
    'friction':5,
    'resitituation':1
    };
    this.body=Bodies.circle(x,y,radius);
    this.width=width
    this.height=height
    world.add(world,this.body)
}
display(){
    var pos=this.body.postion
    var angle=this.body.angle;
    Push();
    translate(pos.x.pos.y);
    rotate(angle);
    ellipseMode(CENTER);
    fill(balck  )
    ellipse(10,20,100,200);
    Pop();

}
}